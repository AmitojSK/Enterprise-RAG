# Local configuration and generated Python files must not enter source control.
.env
# Generated env block for pasting into the Render dashboard; holds real secrets.
/render.env
.venv/
__pycache__/
*.py[cod]
# Local SQLite database created on first run when DATABASE_URL is unset.
data/
.pytest_cache/
.ruff_cache/
coverage.xml
Interview.md
# Session handoff notes for resuming work in a new chat; not part of the project.
HANDOFF.md
# Timestamped evaluation runs; the harness and golden set are tracked, results are not.
evals/reports/
